# Source & build config
src/**
scripts/**
tests/**
docs/**
esbuild.mjs
tsconfig.json
vitest.config.mts
eslint.config.mjs

# Dev state
.vscode/**
.github/**
.git/**
.gitignore
.eslintignore

# Artifacts
**/*.test.ts
**/*.test.js
**/*.map
*.vsix
dist/**/*.map

# Docs scaffolding (only publish README + LICENSE + dist)
README.github.md
SECURITY.md

# Dev-only config & tooling — not needed at runtime
.husky/**
.devcontainer/**
cspell.json
knip.json
playwright.config.ts
.lockfile-lintrc.json
.npmrc

# Marketing assets are for the repo / store listing, not the shipped bundle
marketing/**

# Benchmark / perf outputs
benchmark-*.json

# Runtime deps are bundled into dist/ by esbuild — ship the bundle, not node_modules.
# (better-sqlite3's native addon is external to the bundle but esbuild.mjs copies its
# pruned files into dist/node_modules/, so this rule can stay a blanket exclude.)
node_modules/**

# Local manual-test rules (project-layer, for exercising the trust gate in dev)
.ai-engineer-coach/**
